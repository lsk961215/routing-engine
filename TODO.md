# 구현 TODO

## 목표

작은 지역의 도로 데이터를 자체 엔진에 적재하고, 지도에서 선택한 출발점과 도착점 사이의 경로를 계산한다.

데이터 분석 → 그래프 설계 → 메모리 산정·적재 검증 → 탐색 구현 → API·프론트 연결 순서로 진행한다.

## 현재 상태

- [x] 프론트엔드와 백엔드 모듈 기본 구조 구성
- [x] 프론트 지도 표시 및 OSRM 직접 호출 코드 구현
- [x] Docker 설정과 사용하지 않는 프론트 예제 파일 정리
- [x] 로컬 설정·빌드 결과물 등을 제외하는 `.gitignore` 추가
- [x] 프론트 빌드 검증
- [x] 백엔드 테스트 실행 (`./gradlew test`, 2026-09-22)
- [x] 작은 영역 그래프로 백엔드 서버 실행·API 동작 검증

현재 백엔드 `/api/route`와 `/route`는 작은 영역의 자체 그래프를 탐색한다.
도로 구간 투영·거리 기반 Dijkstra·GeoJSON 응답·프론트 연동을 구현했다.
`data-builder` 모듈 분리, 전체 서울 지원, 속도 기반 시간 계산과 운영 규모 최적화는 남아 있다.

현재 데이터는 `backend/data/raw/south-korea-260919.osm.pbf`(약 274MB)와
`backend/data/processed/seoul-routing.osm.pbf`(약 42MB)다.
`routing-core`의 PBF 분석기가 원본 구조·태그·도로 노드 참조를 집계한다.
[분석 결과 및 메모리 초안](backend/data/ANALYSIS.md)을 기준으로 후속 설계를 진행한다.

## 1. 원본 데이터 분석 — 진행 중

- [x] 서울 샘플의 포맷과 읽기 도구 확인
- [x] 노드·웨이·관계 수와 좌표 범위 집계
- [x] 도로 유형·통행 제한·일방통행·속도 태그 원시 분포 집계
- [x] 도로 고유 노드 ID와 연속 노드 쌍 집계
- [x] 초기 정적 승용차 프로필의 통행 판정·보류 규칙 구현 및 테스트
- [x] 회전 제한 태그와 멤버 역할·종류 집계
- [x] 회전 제한별 누락 참조·멤버 구조·노드 경유 연결 및 방향 검사
- [x] via-way 순차 연결·방향 검증 및 멤버 누락 사유 세분화
- [x] 회전 제한 검증 사례와 처리 정책 문서화
- [x] 전국 원본 대조: 서울 추출로 인한 참조 누락 3건 확인, 나머지 검증 사유는 동일
- [x] 현재 스냅샷의 누락 Way 3개·Node 6개를 별도 서울 보완본에 병합하고 재검증
- [ ] 다른 스냅샷에도 적용할 참조 누락 탐지·보충 자동화
- [ ] 그래프 생성기에 회전 제한 검증 차단 정책 연결
- [x] 정상 via-way 제한의 그래프 변환·이력 기반 탐색·v2 저장 구현
- [ ] 조건부 회전 제한 해석·적용 구현
- [ ] 도로의 연결 지점과 형상 표현용 중간 좌표 구분 기준 검토
- [x] 1차 분석 결과와 확인된 누락·예외 사례 문서화

완료 기준: 실제 데이터의 구조·규모·속성을 근거로 그래프 설계를 시작할 수 있다.

## 2. 탐색용 데이터 구조 설계

- [x] 초기 차량 프로필과 1차 검증 지원 범위 문서화
- [ ] 운영용 통행 규칙 확장 (노드 장벽·크기 제한·목적지 접근 등)
- [ ] 노드·방향성 간선 및 인접 관계 저장 구조 정의
- [ ] 원본 ID와 내부 인덱스 매핑 방식 정의
- [ ] 좌표, 거리, 시간 비용의 타입·단위·정밀도 정의
- [ ] 도로 형상·도로명·회전 제한 저장 방식 정의
- [ ] 좌표를 도로에 연결하기 위한 공간 인덱스 설계
- [ ] 가공 파일의 버전·헤더·검증 정보와 읽기 형식 정의

완료 기준: 필드 정의와 작은 예시 데이터로 도로 연결·형상·제한을 표현할 수 있다.

## 3. 메모리 예산 및 적재 방식 결정

- [x] 전체 highway 노드·연속 노드 쌍으로 배열 메모리 초안 산정
- [x] 초기 차량 프로필 후보 수로 기본 배열 메모리 재산정
- [ ] 회전 제한·공간 인덱스·요청 큐를 포함한 최종 메모리 예산 확정
- [ ] 객체 기반 구조와 기본형 배열 기반 구조 비교
- [ ] 힙 적재와 파일 매핑 방식의 필요성 검토
- [ ] 공통 그래프와 요청별 탐색 작업 공간을 분리해 예산 산정
- [ ] 동시 요청 수에 따른 메모리 증가량 산정
- [ ] 시작 시 적재·검증·준비 완료 처리와 데이터 교체 정책 결정

압축된 원본 파일 크기를 프로세스 메모리 사용량으로 간주하지 않는다. 가공 중 최대 메모리와 서버 실행 중 메모리를 구분한다.

## 4. 데이터 가공 및 로더 구현

- [ ] `data-builder` 모듈 구성 및 서울 샘플 변환 구현
- [ ] 그래프·형상·제한·공간 인덱스 파일 생성
- [ ] `routing-core`에 데이터 로더 구현
- [ ] 서버 시작 시 읽기 전용 공통 그래프 적재
- [ ] 데이터 누락·손상·버전 불일치 처리
- [ ] 참조 인덱스, 방향성, 비용 값 등 데이터 정합성 검증
- [ ] 가공 시간, 적재 시간, 힙·프로세스 메모리 실측

완료 기준: 서울 샘플을 반복해서 가공·적재할 수 있고 측정값이 메모리 예산에 들어온다.

## 5. 경로 탐색 구현

- [x] 작은 테스트 그래프로 진입 간선 기반 Dijkstra 및 경로 복원 검증
- [ ] 클릭 좌표와 도로망 연결 구현
- [ ] 실제 그래프에서 탐색하고 도로 형상 복원
- [ ] 일방통행·통행 제한·회전 제한을 지원 범위에 맞게 반영
- [ ] 경로 없음·고립 구간·동일 출발점과 도착점 처리
- [ ] 거리와 소요 시간의 계산 근거 검증

## 6. API 및 프론트 연결

- [ ] 요청·응답 DTO와 오류 응답 정의
- [ ] 좌표 입력 검증 및 탐색 서비스 연결
- [ ] 프론트의 OSRM 직접 호출을 자체 API 호출로 전환
- [ ] 로컬 개발용 프록시 또는 CORS 설정
- [ ] 로딩·실패·경로 없음 표시 및 중복 요청 처리
- [ ] 지도에서 두 지점 선택 → 탐색 → 경로 표시 흐름 검증

## 7. 이후 개선

- [ ] 정확도·성능 기준과 대표 검증 경로 정리
- [ ] 측정 결과를 바탕으로 A* 등 탐색 최적화 검토
- [ ] 전국 데이터로 가공·적재·탐색 규모 확장 검증
- [ ] 실행 방법, 데이터 준비 방법, 지원 범위 문서화

## 작업 기록 원칙

- 완료 항목은 검증 후 체크한다.
- 설계 결정에는 선택 이유와 측정 근거를 남긴다.
- 지원하지 않는 도로 규칙과 알려진 한계를 명시한다.
- 비밀키, 개인 경로, 실제 개인 이동 기록을 문서에 넣지 않는다.

## 작은 영역 그래프 v1 진행 결과

- [x] 전체 형상 노드 유지 방식의 CSR 방향성 그래프 및 금지 회전 쌍 구현
- [x] 작은 PBF에서 그래프 생성, 미해결 제한 입력 거부
- [x] 버전·CRC·인덱스 검증을 포함한 파일 저장 및 힙 적재
- [x] 설정을 통한 서버 시작 시 공통 그래프 로딩 및 health 확인
- [x] 실제 작은 영역: 노드 79개·간선 103개 생성, 합성 데이터로 제한 보존 검증
- [x] 진입 간선 상태 기반 Dijkstra 및 회전 제한 우회 검증

상세 범위·제약·재현 방법: [GRAPH_V1.md](backend/data/GRAPH_V1.md).
기존 전체 지역 설계·메모리 실측·data-builder 분리 항목은 아직 완료하지 않았다.

## 좌표 API·프론트 초기 연동

- [x] 50m 이내 최근접 노드 연결과 좌표 검증
- [x] `/api/route`에서 자체 Dijkstra 호출 및 geometry·거리 응답
- [x] 프론트 OSRM 직접 호출을 자체 API로 교체, Vite 프록시 구성
- [x] 로딩·오류·요청 취소 처리 및 실제 HTTP 응답 확인
- [x] 간선 중간 투영·방향을 반영한 스냅 및 경계 상자 공간 인덱스 (작은 영역)
- [x] 브라우저 클릭부터 경로 표시·오류·요청 취소까지 검증

최신 좌표 연결 방식·제약·검증은 [GRAPH_V1.md](backend/data/GRAPH_V1.md)의 마지막 절에 기록했다.

## 요청별 메모리·성능 개선

- [x] 전체 그래프 복사를 제거하고 원본 간선 위에서 부분 거리 탐색
- [x] 기존 구현과 250개 경로의 도달 여부·거리 대조
- [x] 실제 작은 그래프 및 합성 격자에서 시간·요청당 할당량 측정
- [ ] 공유 인덱스 유지 메모리·서버 최대 RSS·동시 요청 한도 측정
- [ ] 지원 지역 확대에 앞서 남은 데이터·프로필 제한 해결

측정 기록: [ROUTING_PERFORMANCE.md](backend/data/ROUTING_PERFORMANCE.md).

## via-way 탐색 적용

- [x] 복수 via Way 및 no/only 제한을 간선 시퀀스로 변환
- [x] 간선·이력 상태 기반 탐색, 좌표 부분 경로에도 적용
- [x] 기존 v1 파일 호환 및 제한을 담는 v2 파일 검증
- [x] 실제 관계 8107280의 금지/허용 요청을 CLI·HTTP로 확인
- [ ] 정상 via-way 관계 전체의 컴파일 가능 범위와 규모별 성능 측정

상세: [VIA_WAY_ROUTING.md](backend/data/VIA_WAY_ROUTING.md).
